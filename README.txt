Install NodeJS
    test if installed by using command `node -v` in terminal
    link: https://nodejs.org/en/download/

Optional:
    install `cmder-mini` in your computer
    link: http://cmder.net/


------------------------


LINK: https://github.com/vuejs/vue-cli

Install Vue-cli globally
    run this command in terminal `npm install -g vue-cli`

create project
    go to htdocs and run `vue init webpack-simple vue-ci` (vue init <template-name> <project-name>)
